import 'package:get/get.dart';

// Define an observable RxString to store the user ID.
RxString userID = "".obs;

// Define an observable RxString to store the user's name.
RxString userName = "".obs;

// Define an observable RxString to store the user's email.
RxString email = "".obs;

// Define an observable RxString to store the user's mobile number.
RxString mobile = "".obs;

// Define an observable RxString to store the user's profile URL.
RxString userProfileURL = "".obs;
